
apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-assistant-ui-config
data:
  VITE_API_BASE_URL: "http://ai-assistant-api"  # Internal Kubernetes service name with HTTP
  NODE_TLS_REJECT_UNAUTHORIZED: "0"  # For development only - allows self-signed certs
